# 更新日志 - 多文件类型知识库入库

## 1. 本次新增了什么

本次把知识库上传和自动向量化链路，从原来的只支持 Markdown 和纯文本，扩展为支持 4 类文件：

```text
.md
.txt
.pdf
.docx
```

核心变化是：不再把所有文件都当成普通文本读取，而是根据文件扩展名自动选择对应的“文件类型处理器”。

新链路是：

```text
上传文件
-> 校验扩展名
-> 保存到 uploads
-> 根据扩展名匹配文件类型处理器
-> 处理器读取文件并分片
-> 生成 embedding
-> 写入 Milvus
```

## 2. 以前为什么只支持 md 和 txt

以前上传接口里写死了：

```python
ALLOWED_EXTENSIONS = ["txt", "md"]
```

目录索引里也只扫描：

```python
list(dir_path.glob("*.txt")) + list(dir_path.glob("*.md"))
```

而且单文件索引时统一使用：

```python
content = path.read_text(encoding="utf-8")
```

这种方式只能处理纯文本文件。Markdown 本质也是文本，所以可以处理；但是 PDF 和 Word 是二进制或复杂文档格式，不能直接 `read_text()`。

所以要支持 PDF 和 Word，必须先用专门的 Loader 把文件解析成文本，再分片。

## 3. 新增的文件类型处理器架构

新增文件：

```text
app/services/file_type_handlers.py
```

里面定义了：

```python
BaseFileHandler
MarkdownFileHandler
TextFileHandler
PdfFileHandler
WordFileHandler
FileHandlerRegistry
```

你可以把它理解成一个“文件处理器路由表”：

```text
.md   -> MarkdownFileHandler
.txt  -> TextFileHandler
.pdf  -> PdfFileHandler
.docx -> WordFileHandler
```

每个处理器都实现同一个方法：

```python
load_and_split(file_path)
```

也就是说，不管文件是 Markdown、TXT、PDF 还是 Word，最终都会返回同一种结果：

```python
List[Document]
```

后面的向量化和写入 Milvus 不需要关心原始文件类型。

## 4. 四种文件分别怎么处理

### `.md` Markdown

处理器：

```python
MarkdownFileHandler
```

使用：

```python
MarkdownHeaderTextSplitter
RecursiveCharacterTextSplitter
```

分片方式：

1. 先按 Markdown 标题切：

```text
# 一级标题
## 二级标题
```

2. 保留标题在内容里，同时把标题信息写入 metadata：

```python
h1
h2
```

3. 如果某个标题块仍然太长，再用递归字符分割器继续切。
4. 最后合并太小的 chunk，减少碎片。

适合场景：

- 项目文档
- 运维手册
- FAQ
- 有明显标题结构的知识库

### `.txt` 纯文本

处理器：

```python
TextFileHandler
```

使用：

```python
RecursiveCharacterTextSplitter
```

分片方式：

1. 直接读取 UTF-8 文本。
2. 按递归字符策略切分。
3. LangChain 会优先尝试自然边界，比如段落、换行、空格。

适合场景：

- 简单说明文档
- 日志说明
- 纯文本知识

### `.pdf` PDF

处理器：

```python
PdfFileHandler
```

使用开源方案：

```python
PyPDFLoader
```

新增依赖：

```toml
pypdf>=4.0.0
```

分片方式：

1. `PyPDFLoader` 先解析 PDF。
2. Loader 通常会按页生成 `Document`。
3. 每页内容再进入 `RecursiveCharacterTextSplitter` 二次分片。
4. 不跨页合并小分片，优先保证页码追溯准确。
5. 页码等 loader metadata 会尽量保留。
5. 系统再补充统一 metadata：

```python
_source
_extension
_file_name
```

适合场景：

- PDF 手册
- 产品文档
- 操作指南

注意：`PyPDFLoader` 是轻量方案，适合文本型 PDF。如果 PDF 是扫描件图片，需要 OCR，这不在本次范围内。

### `.docx` Word

处理器：

```python
WordFileHandler
```

使用开源方案：

```python
Docx2txtLoader
```

新增依赖：

```toml
docx2txt>=0.8
```

分片方式：

1. `Docx2txtLoader` 从 `.docx` 提取文本。
2. 提取后的文本再用 `RecursiveCharacterTextSplitter` 分片。
3. 不额外合并小分片，避免 loader metadata 被混淆。
4. 每个 chunk 补充统一 metadata。

适合场景：

- Word 运维文档
- 需求说明
- 项目交接文档

注意：本次支持的是现代 Word 格式 `.docx`，不支持老的 `.doc`。

## 5. 统一 metadata 长什么样

不管是什么文件类型，最终每个分片都会带上：

```python
{
    "_source": "uploads/example.pdf",
    "_extension": ".pdf",
    "_file_name": "example.pdf"
}
```

Markdown 还可能额外带：

```python
{
    "h1": "一级标题",
    "h2": "二级标题"
}
```

PDF loader 还可能带页码相关字段，比如：

```python
{
    "page": 0
}
```

这些 metadata 的作用是后续回答时能知道内容来自哪个文件、哪个章节或哪一页。

## 6. 改了哪些文件

### `app/services/file_type_handlers.py`

新增文件类型处理器和注册表。

核心职责：

```python
file_handler_registry.load_and_split(path)
```

它会根据扩展名选择处理器。

### `app/services/document_splitter_service.py`

新增：

```python
split_loaded_documents(documents, file_path)
```

这个方法专门给 PDF、Word 这类 Loader 使用：

```text
Loader 先解析文件 -> 得到 Document 列表 -> 再二次分片
```

### `app/services/vector_index_service.py`

原来单文件索引会直接：

```python
path.read_text()
```

现在改成：

```python
documents = file_handler_registry.load_and_split(path)
```

目录索引也不再写死 `txt + md`，而是从注册表读取全部支持类型：

```python
file_handler_registry.supported_extensions(include_dot=True)
```

### `app/api/file.py`

上传白名单不再写死：

```python
ALLOWED_EXTENSIONS = ["txt", "md"]
```

现在改成：

```python
ALLOWED_EXTENSIONS = file_handler_registry.supported_extensions()
```

所以只要注册表新增文件类型，上传接口也会自动支持。

### `pyproject.toml`

新增依赖：

```toml
pypdf>=4.0.0
docx2txt>=0.8
```

### `tests/test_file_type_handlers.py`

新增测试：

1. 注册表支持 `.md`、`.txt`、`.pdf`、`.docx`。
2. 每种扩展名能匹配正确处理器。
3. `.doc` 会被拒绝。
4. PDF/Word loader 会被调用。

### `tests/test_vector_index_file_types.py`

新增测试：

1. 目录索引会扫描四种文件。
2. 单文件索引会通过 `file_handler_registry` 分发处理。

## 7. 如何新增第五种文件类型

假设以后要支持 Excel `.xlsx`，只需要：

1. 新增处理器：

```python
class ExcelFileHandler(BaseFileHandler):
    extensions = (".xlsx",)

    def load_and_split(self, file_path: Path) -> List[Document]:
        ...
```

2. 注册到 `FileHandlerRegistry`：

```python
ExcelFileHandler()
```

之后上传接口、目录索引、单文件索引都会自动识别 `.xlsx`。

这就是处理器注册表的好处：新增类型时不需要到处改 if/else。

## 8. 如何验证功能生效

上传文件：

```text
test.md
test.txt
test.pdf
test.docx
```

日志里应该看到类似：

```text
文件类型处理器匹配成功: test.pdf -> PdfFileHandler
Loader 文档分割完成: uploads/test.pdf -> 3 个分片
文件索引完成: uploads/test.pdf, 共 3 个分片
```

如果上传 `.doc`，应该返回不支持：

```text
不支持的文件格式，仅支持: docx, md, pdf, txt
```

## 9. 本次验证结果

已执行静态编译检查：

```bash
python -m py_compile app/services/file_type_handlers.py app/services/document_splitter_service.py app/services/vector_index_service.py app/api/file.py tests/test_file_type_handlers.py tests/test_vector_index_file_types.py
```

如果当前环境缺少新增依赖，需要先安装：

```bash
python -m pip install -e .
```

测试命令：

```bash
python -m pytest tests/test_file_type_handlers.py tests/test_vector_index_file_types.py -q
```
