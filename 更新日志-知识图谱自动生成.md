# 更新日志 - 知识图谱自动生成

## 1. 本次新增了什么

本次在 RAG 知识库入库链路里增加了“自动生成知识图谱”的能力。

原来的入库流程是：

```text
文档上传或目录索引
-> 根据文件类型解析
-> 文档分片
-> 生成 embedding
-> 写入 Milvus 向量库
```

现在变成：

```text
文档上传或目录索引
-> 根据文件类型解析
-> 文档分片
-> 给每个分片补充 chunk_index / chunk_id
-> 调用 Qwen 从分片中抽取实体和关系
-> 写入 SQLite 知识图谱
-> 生成 embedding
-> 写入 Milvus 向量库
```

也就是说，向量库仍然是主链路，知识图谱是增强链路。

## 2. 为什么第一版用 SQLite，不直接上 Neo4j

Neo4j 更适合正式的图数据库查询和可视化，但它会引入新的服务、Docker 配置、连接管理、Cypher 查询和运维成本。

这个项目当前已经有 SQLite 会话存储，所以第一版先用 SQLite 做轻量知识图谱：

```text
kg_entities   存实体节点
kg_relations  存实体之间的关系边
```

这样能先验证三件最重要的事：

1. 文档入库时能不能稳定抽取实体关系。
2. 实体和关系能不能去重。
3. 每条关系能不能追溯到来源文件和 chunk。

后续如果要迁移 Neo4j，只需要把 `kg_entities` 映射成节点，把 `kg_relations` 映射成边。

## 3. 新增配置

在 `app/config.py` 增加：

```python
knowledge_graph_enabled: bool = True
knowledge_graph_db_path: str = "data/knowledge_graph.db"
knowledge_graph_extract_model: str = "qwen-turbo"
knowledge_graph_max_chunk_chars: int = 3000
```

环境变量是：

```text
KNOWLEDGE_GRAPH_ENABLED=true
KNOWLEDGE_GRAPH_DB_PATH=data/knowledge_graph.db
KNOWLEDGE_GRAPH_EXTRACT_MODEL=qwen-turbo
KNOWLEDGE_GRAPH_MAX_CHUNK_CHARS=3000
```

含义：

- `KNOWLEDGE_GRAPH_ENABLED`：是否开启图谱自动生成。
- `KNOWLEDGE_GRAPH_DB_PATH`：SQLite 图谱数据库路径。
- `KNOWLEDGE_GRAPH_EXTRACT_MODEL`：实体关系抽取使用的 Qwen 模型。
- `KNOWLEDGE_GRAPH_MAX_CHUNK_CHARS`：单个 chunk 送给大模型抽取的最大字符数，避免成本失控。

## 4. 实体和关系长什么样

新增模型文件：

```text
app/models/knowledge_graph.py
```

实体结构：

```json
{
  "id": "cpu_high_usage",
  "name": "CPU 使用率高",
  "type": "故障",
  "description": "服务器 CPU 使用率持续处于高位"
}
```

关系结构：

```json
{
  "source": "cpu_high_usage",
  "target": "check_top_process",
  "relation": "排查",
  "description": "CPU 使用率高时需要查看高占用进程",
  "evidence": "查看高占用进程",
  "source_file": "uploads/cpu_high_usage.md",
  "chunk_index": 1,
  "chunk_id": "..."
}
```

其中：

- `source`：起点实体 id。
- `target`：终点实体 id。
- `relation`：关系类型。
- `evidence`：原文依据，必须来自 chunk 内容。
- `source_file`：来源文件。
- `chunk_index`：来源分片序号。
- `chunk_id`：稳定分片 id。

## 5. 大模型如何抽取知识图谱

新增：

```text
app/services/knowledge_graph_extractor_service.py
```

它使用现有阿里云百炼/Qwen 模型：

```python
ChatQwen(
    model=config.knowledge_graph_extract_model,
    api_key=config.dashscope_api_key,
    temperature=0.0,
    streaming=False,
)
```

提示词会要求模型：

1. 只根据原文抽取。
2. 不要编造。
3. 输出严格 JSON。
4. 实体必须包含 `id/name/type/description`。
5. 关系必须包含 `source/target/relation/description/evidence/source_file/chunk_index/chunk_id`。
6. 关系的 `source` 和 `target` 必须引用已抽取的实体 id。

抽取后会做校验：

```text
LLM 原始输出
-> 去掉 ```json 代码块
-> json.loads
-> Pydantic 校验
-> 过滤 source/target 不存在的非法关系
```

## 6. SQLite 如何存图谱

新增：

```text
app/services/knowledge_graph_store_service.py
```

实体表：

```sql
kg_entities (
    entity_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    description TEXT NOT NULL,
    normalized_name TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
)
```

关系表：

```sql
kg_relations (
    relation_id TEXT PRIMARY KEY,
    source_id TEXT NOT NULL,
    target_id TEXT NOT NULL,
    relation TEXT NOT NULL,
    description TEXT NOT NULL,
    evidence TEXT NOT NULL,
    source_file TEXT NOT NULL,
    chunk_index INTEGER NOT NULL,
    chunk_id TEXT NOT NULL,
    created_at TEXT NOT NULL,
    UNIQUE(source_id, target_id, relation, source_file, chunk_index)
)
```

去重规则：

- 实体按 `entity_id` 去重。
- 关系按 `source_id + target_id + relation + source_file + chunk_index` 去重。
- 同一个文件重新入库时，会先删除这个文件旧的关系，再写入新关系。

## 7. 图谱入库服务怎么编排

新增：

```text
app/services/knowledge_graph_index_service.py
```

它负责：

```text
1. 判断 KNOWLEDGE_GRAPH_ENABLED 是否开启
2. 删除该 source_file 的旧关系
3. 遍历每个 Document 分片
4. 补充 _chunk_index 和 _chunk_id
5. 调用抽取服务生成实体关系
6. 写入 SQLite
```

chunk metadata 会新增：

```python
doc.metadata["_chunk_index"] = 1
doc.metadata["_chunk_id"] = "sha256..."
```

这样 Milvus 里的向量文档和 SQLite 里的图谱关系可以通过 `chunk_id` 对齐。

## 8. 原有向量入库怎么改

修改：

```text
app/services/vector_index_service.py
```

原来是：

```python
documents = file_handler_registry.load_and_split(path)
vector_store_manager.add_documents(documents)
```

现在是：

```python
documents = file_handler_registry.load_and_split(path)

if documents and config.knowledge_graph_enabled:
    self._index_knowledge_graph(normalized_path, documents)

vector_store_manager.add_documents(documents)
```

注意：图谱失败不会阻断向量入库。

原因是向量库是主功能，图谱是增强功能。如果某个 chunk 的 JSON 抽取失败，系统会记录 warning，然后继续写 Milvus。

## 9. 如何验证

上传或索引一个文档后，看日志：

```text
知识图谱 SQLite 初始化完成: data/knowledge_graph.db
知识图谱抽取完成: source=..., chunk=1, entities=3, relations=2
知识图谱入库完成: source=..., chunks=5, entities=8, relations=6, failed_chunks=0
文件索引完成: ..., 共 5 个分片
```

也可以查看 SQLite：

```sql
SELECT * FROM kg_entities;
SELECT * FROM kg_relations;
```

重点检查：

1. 实体是否进入 `kg_entities`。
2. 关系是否进入 `kg_relations`。
3. `source_file` 是否是原始文件。
4. `chunk_index` 和 `chunk_id` 是否存在。
5. 重复索引同一文件时，关系不会无限增加。

## 10. 本次新增和修改的文件

新增：

```text
app/models/knowledge_graph.py
app/services/knowledge_graph_extractor_service.py
app/services/knowledge_graph_store_service.py
app/services/knowledge_graph_index_service.py
tests/test_knowledge_graph_extractor_service.py
tests/test_knowledge_graph_store_service.py
tests/test_knowledge_graph_index_service.py
更新日志-知识图谱自动生成.md
```

修改：

```text
app/config.py
app/services/vector_index_service.py
docker-compose.app.yml
README.md
tests/test_vector_index_file_types.py
```

## 11. 后续如何升级 Neo4j

后续可以新增：

```text
app/services/neo4j_knowledge_graph_store_service.py
```

把：

```text
kg_entities -> Neo4j 节点
kg_relations -> Neo4j 边
```

对应 Cypher 大概是：

```cypher
MERGE (e:Entity {id: $entity_id})
SET e.name = $name, e.type = $type, e.description = $description

MATCH (s:Entity {id: $source_id})
MATCH (t:Entity {id: $target_id})
MERGE (s)-[r:RELATED {relation: $relation, source_file: $source_file, chunk_index: $chunk_index}]->(t)
SET r.description = $description, r.evidence = $evidence, r.chunk_id = $chunk_id
```

因为第一版已经把实体和关系结构设计好了，所以迁移 Neo4j 不需要重做抽取逻辑。
