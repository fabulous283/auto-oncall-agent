# 面试题回答稿

## 分块策略有哪些？

如果面试官问我分块策略，我会先说结论：分块没有唯一标准，核心是看文档结构、检索目标和下游模型的上下文容量。常见的分块策略主要有固定长度分块、滑动窗口分块、按语义分块、按文档结构分块，以及混合分块。

固定长度分块最简单，比如每 500 字或 800 字切一段，优点是实现成本低、切分稳定，缺点是容易把完整语义切断，适合格式不稳定、结构信息很弱的纯文本。滑动窗口分块是在固定长度基础上加 overlap，优点是能保留相邻片段之间的上下文，缺点是会带来冗余和存储成本增加，适合上下文连续性比较强的文档。语义分块是根据句子或段落的语义边界来切，优点是语义完整性最好，缺点是实现成本更高、稳定性依赖模型或规则，适合高质量知识库。按文档结构分块是按标题、章节、小节切，优点是天然符合人的阅读方式，特别适合 Markdown、产品文档、运维手册这类结构化文档，缺点是如果某一节过长，还需要二次切分。混合分块就是先结构化分块，再做长度控制，这通常是工程里比较实用的方案。

我这个项目里采用的是结构化分块优先的思路。因为知识库主要是 Markdown 运维文档，本身就有标题层级，所以我优先按标题把一个问题域聚在一起，然后再做二次递归切分，最后再把过小的片段做合并。这样做的目标不是让 chunk 越小越好，而是尽量让一个 chunk 在检索时既保留主题完整性，又不会长到影响召回和后续拼接。

## 优缺点和适用情况是什么？

如果把几种主流策略放在一起比较，我会这样回答。

固定长度分块的优点是稳定、简单、容易批量处理，适合早期原型或者没有明显结构的文本。缺点是语义边界不自然，检索出来的片段经常是“半句开始、半句结束”，后续生成效果一般。

滑动窗口分块的优点是能缓解上下文断裂问题，特别适合连续叙述型文本，比如日志说明、操作步骤、FAQ 长段描述。缺点是同一段信息会重复出现在多个 chunk 中，导致向量数量变多，检索后还可能返回高度重复的片段。

语义分块的优点是召回出来的内容通常更完整、更贴近用户问题，尤其适合复杂问答和高精度知识检索。缺点是对分块算法要求高，且不同语料稳定性不完全一致。

结构化分块的优点是非常适合文档型知识库，因为标题本身就在表达主题边界。像“CPU 高负载排查”“磁盘空间告警处理”这种运维文档，用结构化分块往往比纯长度切分更合理。缺点是如果某个章节特别长，就不能只靠标题切，还得补一个长度切分策略。

所以适用情况上，我一般会建议：

- 纯文本、无结构材料优先考虑固定长度加 overlap。
- 技术文档、Markdown、SOP、手册优先考虑结构化分块。
- 对准确率要求高、数据规模又不是特别夸张时，可以进一步做语义分块或混合分块。

## 分块大小会有什么影响？

分块大小对 RAG 的影响非常大，它会同时影响召回率、精确率、token 成本和最终答案质量。

chunk 太小，优点是检索颗粒度细，容易命中很具体的问题点，但缺点是语义容易不完整，模型拿到一个片段时可能只看到结论看不到前提，或者只看到步骤看不到背景。chunk 太大，优点是上下文更完整，但缺点是向量表达会变“稀”，同一个 chunk 可能混了多个主题，检索精度下降，而且拼给大模型时 token 消耗也会明显上升。

所以 chunk 大小本质上是“语义完整性”和“检索精度”之间的平衡。我这个项目里因为主要处理的是 Markdown 运维知识，所以我的思路不是盲目追求固定长度，而是先按结构聚合，再控制在一个适中的大小范围。这样既能保留像“故障现象、可能原因、排查步骤、解决建议”这种完整局部语义，又不会把整个文档一口气塞成一个大块。

面试里如果继续追问，我会补一句：chunk 大小没有绝对最优值，通常要结合文档类型、embedding 模型能力、检索 topK、LLM 上下文长度一起调。如果知识问答偏事实型，chunk 可以略小；如果偏流程型、操作型，chunk 不能太碎。

## 分块之后怎么存入向量数据库？

分块之后的存储流程我会回答成一个标准的 RAG 入库流程。

第一步是读取原始文档并做清洗，保证文档内容可用。第二步是分块，把文档切成多个语义相对完整的 chunk。第三步是给每个 chunk 生成 embedding 向量。第四步是连同 chunk 的原文、唯一 id 和 metadata 一起写入向量数据库。metadata 通常会存来源文件、标题层级、业务标签、文档类型、时间等信息，便于后续过滤和溯源。

我这个项目里特别重视 metadata，因为知识库不是单纯为了“召回一段文本”，还要回答“这段知识来自哪里、属于哪个主题、能不能用于当前问题”。所以在面试时我会强调：向量数据库里存的不只是向量本身，而是“向量 + 原文 + 结构标签 + 来源信息”的组合。

这样做有两个好处。第一，检索后可以直接把原文片段回传给模型。第二，后续如果要做标签过滤、来源隔离、结果展示、命中分析，都离不开 metadata。

## 语义有相关联的分块怎么减少上下文语义的丢失？

这是一个很典型的面试追问，我通常会从四个层面回答。

第一，用 overlap，也就是让相邻 chunk 有一部分重叠内容，这样前后片段之间不会完全断开。第二，优先按结构切，不要机械地按字符数硬切，因为标题、小节、步骤天然就在定义语义边界。第三，保留层级信息，比如标题、子标题、来源文档，让模型知道这个片段放在什么语境下。第四，在召回阶段不要只拿单个 chunk，可以做相邻片段补充或多片段拼接。

我这个项目的思路就是“结构优先 + overlap + metadata 保留”。如果后续继续优化，我会考虑做相邻 chunk 扩展，也就是命中一个 chunk 后，把前后相邻片段一并带给模型，这对流程型知识尤其有帮助。因为很多运维知识不是一句话问答，而是“现象 + 判断 + 排查 + 处置”连续出现，单片段召回有时会截断语义链。

## RAG的搭建如何实现？

如果面试官问我 RAG 怎么搭建，我会把它拆成离线链路和在线链路两部分。

离线链路负责知识准备。先收集知识源，比如 Markdown 运维文档、FAQ、SOP、排障手册。然后做文档清洗、分块、向量化，最后存入向量数据库，并保存 metadata。

在线链路负责问答。用户提问后，先做 query 理解，再生成查询向量，到向量数据库里召回 topK 个相关片段，把这些片段整理成上下文，最后和用户问题一起送给大模型生成答案。如果是 Agent 场景，还会让 Agent 自主决定什么时候调用检索工具，而不是每次都强制查知识库。

我这个项目就是把 RAG 放进了 Agent 框架里。也就是说，知识检索不是一个孤立模块，而是作为 Agent 可调用的能力存在。这样做的好处是，当用户是普通闲聊时不一定要查知识库；当用户问的是项目原理、运维手册、故障经验时，Agent 会把知识检索作为一个工具拉进来，整个交互会更自然，也更节省资源。

## 有哪些RAG应用测评的框架呢？

RAG 的测评框架现在比较常见的有几类。

一类是像 Ragas 这样的框架，它比较适合评估问答系统在上下文利用、答案相关性、忠实性这些维度上的表现。第二类是 DeepEval，这类框架更适合做 LLM 应用的自动化评测，可以覆盖回答质量、检索质量、链路效果。第三类是 LangSmith 这类偏工程观测和 tracing 的平台，它不只是评测，也适合看链路、看中间结果、做样本对比。再往学术一点走，也会用 BEIR、MTEB 这类基准去评估检索或 embedding 能力。

如果是我这个项目这种学生项目或学习项目，我会优先用两层测评思路。第一层是检索层的离线测评，比如 topK 命中率、召回率、MRR。第二层是问答层的端到端测评，比如答案是否引用到了正确知识、是否出现幻觉、是否覆盖用户关心的信息。这比只看“模型答得像不像”更靠谱。

## 如何测评？

我会把测评分成三层。

第一层是数据层测评，也就是知识库本身是否干净、是否覆盖核心问题、chunk 是否合理。第二层是检索层测评，看用户问题能不能召回正确 chunk。第三层是生成层测评，看模型是不是基于正确上下文给出了准确、完整、可用的回答。

具体来说，检索层我会构建一批标准问答集，每个问题都标注正确文档或正确片段，然后统计 top1 命中率、top3 命中率、MRR、Recall@K。生成层我会看几个指标：答案是否忠于检索内容，是否回答了问题，是否遗漏关键步骤，是否有明显幻觉。

在这个项目里，如果我要做一个比较实用的测评闭环，我会选几类典型问题，比如“CPU 告警如何排查”“慢响应定位流程是什么”“某个项目原理是什么”，给这些问题标标准答案和标准知识来源。然后先验证召回，再验证生成。因为很多时候用户感觉“RAG 不准”，其实问题不一定在大模型，而是前面的 chunk 或召回就错了。

## 工作流是怎么样？

如果回答 RAG 工作流，我会说这是一个“离线建库 + 在线检索增强生成”的流程。

离线阶段，先上传或准备知识文档，做清洗、分块、向量化，并存到 Milvus 这类向量数据库中。在线阶段，用户提问后，系统先理解问题，再从知识库召回相关片段，最后把问题和召回上下文一起交给大模型回答。

在我这个项目里，工作流还多了一层 Agent 决策。也就是说，不是每个问题都强制走 RAG，而是由 Agent 根据用户问题判断是否要调用知识检索工具。如果是知识问答，就触发检索；如果是普通闲聊或工具问题，可以走别的能力。这种设计更贴近真实应用场景。

## 检索怎么进行？

我一般会把检索过程回答成四步。

第一步，把用户问题转成向量。第二步，在向量数据库里根据距离度量找到最相似的 topK 个 chunk。第三步，对召回结果做整理，把原文、来源、标题等信息拼成上下文。第四步，再把这些上下文交给大模型生成最终回答。

当前这个项目的主检索路径是向量检索，不是关键词检索为主。也就是说，它更关注“语义相似”，而不是字面完全匹配。这对于问法变化比较多的场景更合适，比如用户问“CPU 飙高怎么查”，知识库里可能写的是“CPU 高负载故障排查”，两者虽然字面不同，但语义接近，向量检索更容易召回。

## 检索词的选择？

如果是传统搜索，检索词选择很重要；但在向量检索里，更准确地说是“query 质量”很重要。

我的思路是先尽量保留用户原问题的业务语义，不轻易把问题改写得过度抽象。比如用户问“当前 CPU 告警怎么排查”，我会保留“CPU”“告警”“排查”这些业务词，因为它们是检索的关键信号。如果后续发现用户提问很口语化、歧义很大，才会做 query rewrite，也就是把问题改写成更适合检索的表达。

所以在面试里我会说，检索词选择不是拍脑袋选关键词，而是围绕“问题主体、业务对象、异常现象、动作意图”来构造查询。对于复杂问题，也可以拆 query，分别查“症状”“原因”“处理动作”。

## 混合检索有哪些？

混合检索通常指把不同检索方式结合起来。最常见的是“向量检索 + 关键词检索”，也就是 semantic retrieval 和 lexical retrieval 结合。再往后一步，可以加 rerank，对初召回结果进行精排。

比较典型的组合有：

- 向量检索 + BM25
- 向量检索 + 关键词布尔过滤
- 向量检索 + metadata 过滤
- 向量初召回 + reranker 精排

我这个项目当前主路径还是纯向量检索，但如果进一步提升准确率，我优先会做两件事：第一，加 metadata 过滤，让检索先在正确业务域里进行；第二，加混合检索或 rerank。因为有些运维问题同时包含专业词和语义描述，单纯向量检索有时不如“语义 + 关键词”一起做更稳。

## 知识库的选型怎么考虑？

知识库选型我通常从五个方面看。

第一，数据形态。如果主要是文档、手册、FAQ、运维经验库，那更适合 RAG 文档知识库；如果是强结构化数据，可能数据库查询更合适。第二，更新频率。如果知识频繁变化，就要考虑增量更新和重新索引成本。第三，检索方式。如果主要做语义检索，就需要向量库；如果强依赖关键词匹配，可能要混合检索。第四，可解释性。能不能追溯来源、展示引用、支持过滤。第五，工程复杂度和维护成本。

我这个项目选择知识库的思路比较明确，因为它主要承载的是 AIOps 相关 Markdown 文档和项目知识，内容是半结构化文档，不适合直接用 MySQL 做语义检索，所以我会优先考虑“文档切片 + embedding + 向量库”的路线。

## 如果知识库的质量不高，如何保证agent完成任务的准确性？

这是一个非常重要的问题。我会先明确一点：知识库质量不高时，不能把希望全压在大模型身上，必须做多层保护。

第一层是知识入库前治理，包括去重、清洗、统一术语、剔除过期内容。第二层是检索约束，不要让模型拿到一堆低相关片段胡乱生成。第三层是答案生成约束，比如要求模型优先基于已检索内容回答，缺乏依据时要明确说明。第四层是任务拆解，对于高风险任务，不要让 Agent 一步到位，而是拆成检索、判断、执行建议几个阶段。

在我这个项目里，如果知识库质量不够高，我会优先采用“检索结果显式引用 + Agent 保守回答 + 多轮确认”的策略。尤其是在运维场景下，如果知识依据不足，我更倾向于让系统先给出排查建议和信息缺口，而不是强行输出一个看似完整但不可靠的结论。

## 不同的嵌入模型对于检验召回结果的影响？

Embedding 模型会直接影响召回质量，因为向量检索的基础就是它输出的向量空间。

如果 embedding 模型对中文理解不强、对技术术语区分能力弱，召回结果就容易出现“字面看起来相关，但业务上不相关”的问题。反过来，如果模型在中文、专业术语、多句语义表达上更强，它更容易把真正相关的问题和文档拉近。

所以不同 embedding 模型对召回结果的影响主要体现在三个方面：第一，语义区分能力；第二，领域术语适配能力；第三，向量空间稳定性。这个项目当前选用的是 `text-embedding-v4`，主要是因为它在工程接入上稳定、对中文语义检索足够实用，并且和当前技术栈兼容。如果后续升级，我会优先考虑更强的新一代中文检索模型，但前提是要重新评估整库召回效果，不能只看模型名更新就直接替换。

## 怎么验证知识召回的准确率和达标率？

我会从“准确率”和“达标率”两个角度来讲。

准确率重点看召回结果里有没有正确知识。做法是先准备一个带标准答案的测试集，每个问题标注正确文档或正确 chunk，然后统计 top1、top3、top5 的命中情况。

达标率更偏业务视角，也就是系统是否达到我预期的最低可用标准。比如我可以定义：top3 召回命中率达到 85% 以上，或者高频问题在 top1 命中率达到 70% 以上，这就算达标。这个阈值不能凭感觉定，要根据项目目标和知识复杂度来设。

如果是我来做这个项目的评估，我会选一批真实高频问题，用人工标注出标准知识来源，然后统计 Recall@K、MRR，再结合人工抽样看召回片段是不是足够支持回答。因为有时候虽然“命中了文档”，但命中的 chunk 太碎，实际上还是没法回答。

## 如何优化召回率？

优化召回率我一般按链路拆。

第一，优化分块，让一个 chunk 既完整又聚焦。第二，优化 query，比如做 query rewrite、问题拆分、同义词归一。第三，优化 embedding 模型，提升语义表达能力。第四，调 topK，不要过小。第五，加混合检索或 rerank。第六，用 metadata 过滤降低噪声。

在我这个项目里，如果我要实打实提升召回率，我会优先做三件事。第一，继续优化 chunk 设计，尤其是运维知识里的步骤型内容。第二，补充 metadata 标签，比如故障类型、资源类型、告警类别。第三，引入混合检索或 rerank。因为当前项目主路径已经能跑通，但如果要追求更高召回质量，这三块是性价比最高的。

## Milvus向量数据库的内部存储结构是什么？

如果面试官这样问，我不会陷进特别底层的源码细节，而是从工程上回答它的存储组织。

Milvus 可以理解成是为向量检索设计的数据存储和索引系统。对我这个项目来说，我把每条数据看成一条向量记录，这条记录至少包含主键 id、向量字段、原文内容和 metadata。向量字段用于相似度搜索，原文和 metadata 用于回传和过滤。

从工程认知上讲，Milvus 内部会围绕 collection、field、segment、index 来组织数据。collection 类似一张向量表，field 是字段定义，segment 可以理解成底层数据组织单元，index 是为了提高 ANN 检索效率建立的结构。面试里这样回答已经足够专业，而且不会说得过虚。

## 它的索引是什么？

我这个项目当前使用的是 IVF_FLAT 索引，距离度量用的是 L2。

我会这样解释：IVF_FLAT 属于典型的近似最近邻检索方案，它会先把向量空间划分成多个簇，检索时先找到最可能相关的簇，再在这些簇里做精确比较。这样比全量暴力搜索快很多，同时又比特别激进的压缩类索引更稳。

之所以在这个项目里选它，是因为这是一个学习项目，知识规模还没有大到必须上特别复杂的索引结构，而 IVF_FLAT 在实现复杂度、检索性能和结果稳定性之间比较均衡。

## 做的时候有没有做哪些索引设置调优的工作？

有，我会回答我做过基础调优，但不会夸大成大规模生产级调优。

我在这个项目里的调优重点主要是三块：第一，统一向量维度，避免 embedding 维度和 collection schema 不一致。第二，索引类型选择上采用 IVF_FLAT，而不是一上来就用更复杂的压缩索引。第三，参数上配置了 `nlist` 和查询侧的 `nprobe`，这是 IVF 系列比较关键的两个参数。

如果面试官继续问深一点，我会明确说：这是一个学习项目，所以调优思路是基于小中规模知识库的“先稳定、再优化”，重点是保证检索正确性，而不是盲目追求极限吞吐。

## 如何做索引设置调优？

索引调优我会从“准确率、延迟、资源占用”三者平衡来回答。

以 IVF_FLAT 为例，`nlist` 可以理解为聚类桶的数量，`nprobe` 可以理解为查询时要探测多少个桶。一般来说，`nlist` 越大，空间划分越细，但建索引和维护成本会增加；`nprobe` 越大，召回更全，但查询延迟会上升。

所以调优方法通常是：

- 固定测试集，先看 Recall@K
- 再看查询时延
- 在不同 `nlist`、`nprobe` 组合下找一个平衡点

如果数据规模再上去，还要考虑 segment 数量、分片、加载策略、机器内存等问题。我的项目里目前是以验证功能正确为主，所以索引调优更多是基础参数调优，而不是大规模线上压测。

## 支持哪些距离度量的方式？

向量数据库常见的距离度量方式主要有欧式距离 L2、内积 IP 和余弦相似度。不同度量方式适合的 embedding 模型和场景不完全一样。

我这个项目当前用的是 L2，也就是欧式距离。原因是现有索引和检索链路就是按这个度量统一配置的。面试里我会补一句：距离度量没有绝对好坏，关键是要和 embedding 模型的训练方式、归一化策略以及离线评测结果保持一致。最终不是拍脑袋选，而是看哪种度量在项目测试集上表现最好。

## 当数据很多的时候，检索的瓶颈是什么？

当数据量上来以后，检索瓶颈通常不止一个。

第一是索引本身是否适合当前规模，如果还是比较粗糙的索引，延迟会上升。第二是向量维度和数据量共同带来的内存压力。第三是 segment 太多、数据分布不均、加载策略不合理。第四是查询侧参数过大，比如 `nprobe` 太高。第五是检索之后的结果处理，比如 rerank 或大段上下文拼接，也可能成为瓶颈。

所以我会告诉面试官，数据量大时不能只盯着“Milvus 慢了”，而是要把链路拆开看：embedding 生成、向量检索、过滤、回表、rerank、LLM 生成，每一步都有可能成为瓶颈。

## 查询变慢了，可以怎么调优？

如果查询变慢，我会按排障思路来优化。

先看是不是索引参数问题，比如 `nprobe` 过大导致每次查太多簇。再看 collection 是否合理加载到内存。然后看数据量和 segment 情况，是否需要做更合适的索引选择或数据整理。再往后看 metadata 过滤条件是否过重，或者检索后拼接上下文太长。

如果是我这个项目，我会优先排查四件事：第一，是否 topK 设得过大；第二，是否 `nprobe` 过高；第三，是否知识库 chunk 太碎导致向量条数膨胀；第四，是否召回后给大模型的上下文太长，用户感知到的是整体响应慢，而不一定是 Milvus 本身慢。

## 为什么选择这个？

如果这里指的是为什么选择 Milvus，我会回答：因为这个项目是典型的 RAG 语义检索场景，需要一个专门处理高维向量检索的数据库，而不是传统关系型数据库。

我选择 Milvus 的原因主要有四个。第一，它就是为向量检索设计的，语义搜索能力比 MySQL 这类传统数据库路线更合适。第二，它和 LangChain、Python 生态结合比较自然，适合快速搭建学习项目。第三，它支持主流 ANN 索引和距离度量，后续可扩展。第四，它在工程上已经是比较成熟的向量库，便于我把重点放在 RAG 链路本身，而不是自己造轮子。

## 什么是embedding模型？

Embedding 模型可以理解成“把文本变成向量”的模型。这个向量不是随便的一串数字，而是尽量把文本语义编码进高维空间里，让语义相近的文本在向量空间中也更接近。

比如“CPU 高负载如何排查”和“CPU 告警怎么分析”字面并不完全一样，但语义接近。经过 embedding 后，这两个句子的向量距离通常会更近。这样系统就可以不用只依赖关键词匹配，而是做语义检索。

所以在 RAG 里，embedding 模型的作用就是把“知识片段”和“用户问题”映射到同一个语义空间里，后续向量数据库才能做相似度搜索。

## 为什么选择这个？

如果这里问的是为什么选择当前这个 embedding 模型，我会回答成工程选择而不是绝对最优选择。

我这个项目当前使用的是 `text-embedding-v4`，主要考虑三点。第一，它对中文语义检索已经足够实用，适合这个学习项目当前的数据规模和复杂度。第二，它和现有技术栈兼容，接入成本低，工程上比较稳。第三，我这个项目当前更重视整条 RAG 链路打通，包括分块、入库、召回、Agent 调用，而不是一开始就追求最前沿 embedding 模型。

如果后续迭代，我会重新做评测，再决定是否升级到更强的新一代中文检索模型。因为 embedding 模型一旦切换，不只是改一个名字，往往意味着整库向量都要重建，所以必须用效果数据来驱动，而不是只看模型新旧。

## 原理是什么？

如果继续追问 embedding 的原理，我会这样回答。

它的本质是用神经网络把文本编码成一个高维向量。这个向量会尽量保留文本的语义特征，使得语义接近的句子在向量空间中距离更近，语义差异大的句子距离更远。这样在检索时，就可以把用户问题也编码成向量，然后通过距离计算找到最接近的知识片段。

在工程应用里，我们不会自己训练这个模型，而是调用成熟的 embedding 模型服务，把它作为“语义表示层”来用。RAG 的检索质量很大程度上取决于这个语义表示层是否稳定、是否适合当前语言和领域。

## rag的标签化空间隔离是怎么回事？

我会把“标签化空间隔离”解释成一种知识检索的范围控制机制。

它的核心思想是：不是所有知识都放在一个完全开放的检索空间里，而是根据业务域、来源、租户、系统模块、文档类型等信息打标签，在检索时先用标签把搜索空间缩小，再在这个更小、更干净的子空间里做向量匹配。

这样做的价值很大。第一，减少噪声，避免 A 系统的问题召回到 B 系统的文档。第二，提高准确率，因为相似度比较是在更同质的数据集合里进行。第三，便于做权限或场景隔离。第四，便于溯源和治理。

如果结合我这个项目来回答，我会说我理解的标签化空间隔离主要是通过 metadata 来做，比如来源文件、标题层级、业务域、告警类型、资源类别等。当前这个项目的主检索仍然是统一知识库上的向量检索，但如果要进一步提升准确率，我会优先把标签过滤加上，让知识检索先限定在正确的业务域里，再做语义召回。这样比单纯扩大 topK 更有效。

